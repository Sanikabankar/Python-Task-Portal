# Python-Task-Portal
Python Task Portal is a lightweight, local task management system built with Python. The program runs a local server and generates a link, which can be opened in a web browser (like Chrome) to access a simple and interactive To-Do List page.
